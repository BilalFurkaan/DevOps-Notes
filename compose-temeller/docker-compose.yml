services:
  web:
    image: nginx
    ports:
      - "8080:80"
    depends_on:
      - db

  db:
    image: postgres:17
    environment:
      POSTGRES_PASSWORD: sifre123
    volumes:
      - db-data:/var/lib/postgresql/data

volumes:
  db-data:
